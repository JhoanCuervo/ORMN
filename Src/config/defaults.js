/**
 * @module config/defaults
 * @description Opciones por defecto del ORM.
 */

const defaults = {
    includeSpreadsheetMeta: true,
    includeSheetMeta: true,
    includeRowIndex: true,
    enableDelete: true,
    enableDeleteMany: true,
    enableDeleteAll: true,
    enableSave: true,
};

export default defaults;
